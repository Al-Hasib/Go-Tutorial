package main

import "fmt"

func main() {

	//slices
	// dynamic length, flexible, dynamic view of an array

	// var slice []int
	// fmt.Println(slice)
	// fmt.Println(len(slice))
	// fmt.Println(slice == nil)
	// fmt.Println(cap(slice))
	

	// // //slice literal
	// numbers := []int{1, 2, 3, 4, 5}
	// // fmt.Println(numbers)
	// // fmt.Println(len(numbers))
	// // fmt.Println(cap(numbers))

	// // //slicing an array
	// arr := [...]int{1, 2, 3, 4, 5}
	// slice2 := arr[1:4]
	// fmt.Println(slice2)


	// // //slicing a slice
	// slice3 := numbers[1:3]
	// fmt.Println(slice3)

	// // //appending to a slice
	// slice4 := append(slice3, 6, 7, 8)
	// fmt.Println(slice4)

	// // //updating a slice
	// slice4[0] = 10
	// fmt.Println(slice4)

	// // //deleting from a slice
	// slice5 := append(slice4[:2], slice4[3:]...)
	// fmt.Println(slice5)

	// //slicing with make
	// slice6 := make([]int, 5, 10)
	// fmt.Println(slice6)
	// fmt.Println(len(slice6))
	// fmt.Println(cap(slice6))

	// // // appending to a slice with make
	// slice6 = append(slice6, 1, 2, 3, 4, 5, 6)
	// fmt.Println(slice6)
	// fmt.Println(len(slice6))
	// fmt.Println(cap(slice6))

	// //copying a slice
	// slice7 := []int{1, 2, 3, 4, 5}
	// slice8 := make([]int, len(slice7))
	// copy(slice8, slice7)
	// fmt.Println(slice8)





}
